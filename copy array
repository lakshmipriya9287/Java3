import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        
        // Read size of array
        int N = sc.nextInt();
        
        // Declare original and copied arrays
        int[] original = new int[N];
        int[] copied = new int[N];
        
        // Read array elements
        for (int i = 0; i < N; i++) {
            original[i] = sc.nextInt();
        }
        
        // Copy elements
        for (int i = 0; i < N; i++) {
            copied[i] = original[i];
        }
        
        // Print copied array
        for (int i = 0; i < N; i++) {
            System.out.print(copied[i] + " ");
        }
        
        sc.close();
    }
}
